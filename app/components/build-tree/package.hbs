{{#if this.hasBuild}}
  has build
  <LinkTo @route="ecosystem.builds.build" @model={{this.build}}>
    "{{this.build.status}}" ({{this.build.profile.name}})
  </LinkTo>
  {{#if this.hasFailedBuild}}
    <BuildTree @build={{this.build}} />
  {{/if}}
{{else}}
  no build
{{/if}}
<ul>
  {{#each @package.requires as |requirement|}}
    <BuildTree::PackageNode @present={{true}} @package={{requirement}} />
  {{/each}}
</ul>