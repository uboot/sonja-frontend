{{page-title "Ecosystem"}}

<section>
  <h3>Channels</h3>

  <p>
    Conan <em>channels</em> are the last part of the Conan package reference, e.g.
    <code>stable</code> in <code>hello/1.0.0@mycompany/stable</code>. A channel
    is assigned to a git branch or a git branch pattern. For each commit to the
    channel branch in a repository a new Conan package will be built in the 
    channel.
  </p>

  <LinkTo @route="ecosystem.channels.new" class="btn btn-primary">New Channel</LinkTo>
</section>

<section>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col" colspan="3">Branches</th>
      </tr>
    </thead>
    <tbody>
      {{#each this.model.channels as |channel|}}
        <tr>
          <td>{{channel.name}}</td>
          <td>{{channel.branch}}</td>
          <td><LinkTo @route="ecosystem.channels.channel" @models={{array channel.ecosystem channel}} class="btn btn-primary btn-sm">Open</LinkTo></td>
          <td><RemoveDialog @item={{channel}} class="btn-sm"/></td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</section>
