<section>
  <LinkTo @route="ecosystem.settings.profiles.new" class="btn btn-primary">New Profile</LinkTo>
</section>

<section>
  <table class="table">
    <thead>
      <tr>
        <th scope="col">Name</th>
        <th scope="col" colspan="3">Container Image</th>
      </tr>
    </thead>
    <tbody>
      {{#each this.model.profiles as |profile|}}
        <tr>
          <td>{{profile.name}}</td>
          <td>{{profile.container}}</td>
          <td><LinkTo @route="ecosystem.settings.profiles.profile" @models={{array profile.ecosystem profile}} class="btn btn-primary btn-sm">Open</LinkTo></td>
          <td><RemoveDialog @item={{profile}} class="btn-sm"/></td>
        </tr>
      {{/each}}
    </tbody>
  </table>
</section>
